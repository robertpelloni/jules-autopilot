# Handoff Archive — 2026-04-05 — v1.0.1

## Snapshot
- Canonical version source normalized to `VERSION`
- Compatibility `VERSION.md` added and synchronized
- Web UI, CLI, daemon manifest, and package manifests synchronized to `1.0.1`
- Hypercode cloud webhook compatibility merged (`/api/webhooks/hypercode` + `/api/webhooks/borg`)
- `lib/jules/client` now supports `process.env.VITE_JULES_API_BASE_URL` outside a Vite runtime
- Jest restored with the working CJS `ts-jest` configuration
- Shared orchestration tests and `lib/jules/client.test.ts` are passing
- Lint still blocked by missing ESLint flat config
- Live Prisma WAL/SHM intentionally left untouched to avoid disrupting active processes

## Validation
- `pnpm run test` ✅
- `pnpm run typecheck` ✅
- `node scripts/check-version-sync.js` ✅
- `pnpm run lint` ❌ missing `eslint.config.js`

## Operational Note
No processes were killed. Live database sidecar files were intentionally left unstaged.
