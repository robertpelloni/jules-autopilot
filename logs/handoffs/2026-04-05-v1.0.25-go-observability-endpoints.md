# Handoff Archive — v1.0.25 Go Observability Endpoints

## Summary
This pass added Go-native observability foundations with Prometheus-style metrics and structured health endpoints.

## Shipped
- Added `GET /metrics` in the Go backend.
- Added `GET /healthz` in the Go backend.
- Added `GET /api/health` in the Go backend.
- Added Go daemon running-state introspection for health/metrics reporting.
- Bumped version surfaces to `1.0.25`.

## Why This Matters
This starts delivering the planned observability and health-check milestone in the Go runtime, not just parity work.

## Validation
- `cd backend-go && gofmt -w api/routes.go services/daemon.go services/jules_client.go && go test ./...`
- `pnpm run lint`
- `pnpm run typecheck`
- `pnpm run test`
- `node scripts/check-version-sync.js`

## Recommended Next Step
- Tighten provider abstractions and/or build a dashboard surface for the new health data.
