# Handoff Archive — v1.0.28 Go Websocket Protocol Parity

## Summary
This pass aligned Go websocket protocol behavior with the Bun daemon more closely by emitting `connected` on open and replying to client `ping` frames with `pong` payloads.

## Shipped
- Updated `backend-go/main.go` websocket behavior.
- Added `connected` event emission on websocket open.
- Added protocol-compatible `pong` replies to client `ping` messages.
- Removed arbitrary websocket echo behavior.
- Bumped version surfaces to `1.0.28`.

## Why This Matters
At this stage, important remaining migration gaps are often subtle runtime semantics. Websocket protocol alignment improves Go drop-in daemon/runtime parity.

## Validation
- `cd backend-go && gofmt -w main.go && go test ./...`
- `pnpm run lint`
- `pnpm run typecheck`
- `pnpm run test`
- `node scripts/check-version-sync.js`

## Recommended Next Step
- Continue auditing for any remaining Bun-only runtime behavior still worth porting.
