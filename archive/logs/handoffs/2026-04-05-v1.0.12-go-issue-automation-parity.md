# Handoff Archive — v1.0.12 Go Issue Automation Parity

## Summary
This pass ported Go-side GitHub issue scanning and autonomous Jules session spawning, completing the primary queue-job migration set.

## Shipped
- Added GitHub issue fetching support to `backend-go/services/jules_client.go`.
- Added Go-side Jules session creation support to `backend-go/services/jules_client.go`.
- Ported `handleCheckIssues` into `backend-go/services/queue.go`.
- Added hybrid issue evaluation:
  - OpenAI JSON triage when available
  - conservative heuristic fallback otherwise
- Added Keeper log coverage for issue scan start, issue evaluation, and autonomous session spawning.
- Expanded `POST /api/fleet/sync` to enqueue issue-check jobs and a codebase indexing job in addition to memory-sync jobs.
- Bumped version surfaces to `1.0.12`.

## Why This Matters
The Go backend now owns the three most important background job classes that previously defined the TypeScript queue's practical autonomy surface:
- `check_session`
- `index_codebase`
- `check_issues`

## Validation
- `cd backend-go && go test ./...`
- `pnpm run lint`
- `pnpm run typecheck`
- `pnpm run test`
- `node scripts/check-version-sync.js`

## Remaining Gap
- provider-backed council debate parity for risky plan approval
- semantic query/RAG retrieval parity on top of Go-owned indexing
- broader lifecycle event detail parity

## Recommended Next Step
- Port provider-backed council debate parity in Go.
