# Handoff Archive — v1.0.17 Go Session Read and RAG Reindex Parity

## Summary
This pass closed several remaining practical Go route gaps by adding direct session read, direct session-activity read, and Go-native RAG reindex triggering.

## Shipped
- Added `GET /api/sessions/:id` to the Go API.
- Added `GET /api/sessions/:id/activities` to the Go API.
- Added `POST /api/rag/reindex` to the Go API.
- Bumped version surfaces to `1.0.17`.

## Why This Matters
The Go backend now covers a more complete practical session-control and memory-control surface, reducing remaining route-level parity friction with the Bun daemon.

## Validation
- `cd backend-go && go test ./...`
- `pnpm run lint`
- `pnpm run typecheck`
- `pnpm run test`
- `node scripts/check-version-sync.js`

## Remaining Gap
- recovery edge-case refinement
- provider/runtime abstraction polish
- audit of non-core product surfaces for Go migration

## Recommended Next Step
- Refine Go recovery state tracking and audit non-core product surfaces.
