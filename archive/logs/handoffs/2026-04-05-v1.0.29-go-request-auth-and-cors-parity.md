# Handoff Archive — v1.0.29 Go Request Auth and CORS Parity

## Summary
This pass added request-scoped Jules auth header handling and Bun-like permissive CORS behavior to the Go runtime.

## Shipped
- Added request-aware Jules client resolution in `backend-go/api/routes.go`.
- Applied request-scoped auth support to practical session-facing Go routes.
- Added Fiber CORS middleware in `backend-go/main.go` with Bun-like header/method allowances.
- Bumped version surfaces to `1.0.29`.

## Why This Matters
This improves real deployment/runtime flexibility for the Go backend and closes another practical difference between Go and Bun behavior.

## Validation
- `cd backend-go && gofmt -w main.go api/routes.go && go test ./...`
- `pnpm run lint`
- `pnpm run typecheck`
- `pnpm run test`
- `node scripts/check-version-sync.js`

## Recommended Next Step
- Continue auditing any remaining Bun-only deployment/runtime assumptions.
